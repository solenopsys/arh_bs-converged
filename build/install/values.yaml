replicasCount: 1
fullName: "richteri-hcf-hub"
selectorLabel: "richteri-hcf-hub"

service:
  httpPort: 80

container:
  name: nginx
  image: registry.richteri.solenopsys.org/sc-front-hub
  pullPolicy: Always
